# تقرير إصلاح واختبار: Before/After + الفلاتر والضبطيات والتعريض + AI

**المشروع:** VISTON Studio
**التاريخ:** 2026-09-21
**النطاق المغلق:** (6) Before/After، (7) الفلاتر + الضبطيات + التعريض، (9) AI، (10) طبقة AI، (11) AI المحلي.

---

## 1) الملخص التنفيذي

أُنجز النطاق المغلق دون أي تغيير في الشيفرة الإنتاجية؛ إذ أثبت التدقيق أنّ البنية الحالية صحيحة وقابلة للالتزام بها،
واقتصر العمل على تحقق (Verification) وتوثيق واختبارات مقفلة (Tests). لا توجد أي نتائج مُزيّفة: كل تنفيذ AI حقيقي يمرّ عبر
Service→Provider→Model، والمزوّد المحلي صريح بأنه غير متاح، والمزوّد التجريبي (Mock) محصور في وضع التطوير مع تفعيل
صريح ووسم دائم `origin:"mock"`. التعريض موحّد في كل المسارات على الصيغة الرياضية `Math.pow(2, EV)`.

**النتائج التفصيلية:**
- typecheck: `PASS`
- الاختبارات: `30 ملف / 272 اختبار PASS` (الخط الأساسي كان 29 ملف / 250 اختبار)
- البناء الإنتاجي: `PASS` (index الجاهز 474.49 kB، مكافئ للخط الأساسي)

---

## 2) تدقيق Before/After

**الوضع الحالي (تم التحقق منه):**
- مرجع "قبل" (Before) = حالة المستند الأصلية عند الفتح، تُلتقط من الكومبوست عبر `compositeToCanvas` ثم
  `downscaleCanvas(COMPARE_MAX_REF_DIM=4096)` — `engine.ts:2360–2381`.
- الالتقاط يتم مرة واحدة لكل مستند (مفتاح الوثيقة) ولا يُعاد إلا عند استيراد صورة جديدة (`engine.ts:1192`).
- `setCompare` هو حالة واجهة خالصة ولا يمسح الوثيقة أو السجل أو العلم dirty (`store.ts:579`).
- واجهة المقارنة: `BeforeAfterViewer` + `compareModel` بوضعيات side-by-side / split / overlay.

**القرار:** لا حاجة لإصلاح في الشيفرة. أُضيفت مصفوفة استقرار عبر الميزات (6 اختبارات) تثبت أنّ "قبل" ثابت ولا يتغير عند:
إضافة الضبطيات (`adjustmentsApply`)، تطبيق التعريض، التشكل المورفولوجي، الرسم على الأقنعة، وتبديل وضع المقارنة دون أي
تغيير في الوثيقة/العلم dirty/النسخة/السجل.

---

## 3) تدقيق الفلاتر والضبطيات (الجرد الكامل)

- **الفلاتر (ProcessOp):** 50 مفتاحاً مواصفاتياً:
  - ثلاثية الضبابية (blur ×3)، 11 لتقوية الحدة (sharpen)، 7 للحواف (edge)، 1 للضوضاء (noise)،
    5 لأسرة المتوسط، 15 للتشكل المورفولوجي، 7 ضبطيات منفردة + الضبطية المركّبة `adjustments`.
- **أنواع الضبطيات (ADJUSTMENT_KINDS):** 14 — brightness، contrast، gamma، saturation، exposure، hue،
  temperature، vibrance، colorBalance، shadows، highlights، levels، curves، equalize.
- **المسارات:** `effectApply(op, params, name)` و `adjustmentsApply(values)` في المحرك، وطبقات الضبط عبر
  `createAdjustmentLayer(adjustment, amount, name, params)` (الواجهة تُنشئ فعلياً طبقات levels/curves فقط).

**القرار:** لا إصلاح مطلوب؛ الجرد أُنشئ لتوثيق النطاق والاختبارات.

---

## 4) التعريض — التوحيد إلى معامل مضاعف 2^EV

**الوضع السابق (المفترض/المدعّى):** كان الافتراض السابق أنّ التعريض يستخدم صيغة تضييق `brightness(amount/100)`
المختلفة عن الضبطيات. **النتيجة: هذا الافتراض غير صحيح** — لا يوجد أي مسار بسلوك تضييق من هذا النوع.

**الوضع الجديد (المثبَّت بالاختبارات):** التعريض موحّد في **كل** المسارات على الصيغة المضاعفة عمودياً `Math.pow(2, amount)` حيث `amount` بوحدة EV:

| المسار | الموقع | الصيغة |
|---|---|---|
| معالج البكسلات | `processor.ts:355-368` (exposure) | `2^amount` |
| خط الضبطيات | `processor.ts:421` (expFactor في runAdjustments) | `2^(EV)` |
| الكومبوستر | `compositor.ts:208` (mult في applyAdjustmentPixels) | `2^amount` |
| العامل (worker) | `processWorker.ts:15` → `runProcess` | نفس المعالج |
| المعاينة (preview) | `processingEngine.processCanvas` → `run` | نفس المعالج |
| النتيجة النهائية | compositor أعلاه | `2^amount` |
| خط إنتاج المنتجات | قيم EV من -1..1 | نفس الصيغة |
| حفظ المشروع | `projectFormat.ts` | قيم EV من -3..3 |
| واجهات الإدخال | فلاتر EV -5..5، نافذة الضبطيات EV -5..5، الوحدة EV | نفس الصيغة |

**الاختبارات الجديدة المقفلة (في `exposure.test.ts`):**
1. **مسار تشغيل موحّد:** مسار خطوة التشغيل (الخيط الرئيسي عبر `processingEngine.run`) مطابق بايتاً-ببايت لمسار العامل
   `runProcess` لنفس البكسل ونفس القيمة.
2. **تطابق الكومبوستر:** طبقة الكومبوستر (عنصر صورة + طبقة ضبط تعريض عبر `compositeToCanvas` الحقيقي) تطابق معالج
   البكسلات ضمن تفاوت ±1 لكل قناة RGB (سبب التفاوت: `Uint8ClampedArray` يستخدم تقريب نصف-زوجي بينما المعالج يستخدم
   `Math.round`)، وقناة alpha متطابقة تماماً.
3. **استقرار الوحدات المظلمة:** وحدات مظلمة تبقى مظلمة تحت تعريض +1.5 EV (لا انزلاق/تجمد مثل).
4. **الهوية عند 0:** `amount=0` لا يغيّر البكسل.
5. **القيم القصوى:** `±100` و `±200` تبقى محددة الصلة ومحدودة عددِياً (finite) ولا تمسّ قناة alpha أبداً.

---

## 5) تقرير العامل (Worker)

عامل المعالجة (`processWorker.ts:15`) يفوّض مباشرة إلى `runProcess` (النفس المستخدمة في المسار الاحتياطي بالخيط الرئيسي)
دون إعادة تنفيذ لأي دالة. لذلك عددياً **لا يوجد انحراف بين المعاينة والنتيجة النهائية** — أُثبت ذلك بالاختبار (قسم 4/بند 1).

---

## 6) تقرير المعاينة (Preview)

المعاينة تستخدم نفس `processingEngine` مع حد أقصى `PREVIEW_MAX_DIM=512` وتُشغّل نفس `runProcess` (مع مسار احتياطي في
الخيط الرئيسي إذا تعذّر العامل). النتيجة: المعاينة والنتيجة النهائية تشتركان في نفس الرياضيات فلا يوجد "انزلاق معاينة".

---

## 7) تقرير العرض النهائي

الكومبوستر (`compositor.ts`) يطبّق حالة التعريض عبر `applyAdjustmentPixels` (mult = 2^amount) داخل بناء LUT بالمسار
النهائي. فرق وحيد تم توثيقه: `buildLut` ينسخ عبر `Uint8ClampedArray` (تقريب نصف-زوجي) مقابل `Math.round` في المعالج،
ولهذا سمح الاختبار بتفاوت ±1 في RGB فقط، مع مضاعفة دقيقة لـ alpha.

---

## 8) تدقيق خدمة AI (البنية)

- **Service→Provider→Model:** كل مسارات الواجهة (لوحة AI، الأوامر `aiCommands.ts`) تستدعي `aiService.run` فقط؛ لا يوجد
  أي استدعاء شبكي مباشر خارج `RemoteAIProvider.execute`.
- **تسجيل المزوّدين:** `LocalAIProvider` + `RemoteAIProvider` دائمين؛ `MockAIProvider` **لا يُسجَّل إلا في وضع التطوير**
  (DEV) مع تفعيل صريح من الإعدادات (`AIService.ts:77-81`).
- **إدارة المهام:** `AIJobManager` — طابور متسلسل (max Concurrent=1) لا يحجب الواجهة، يدعم الإلغاء الفردي والكلي،
  التقدم، والتقليم، وخرائط الأخطاء إلى `AIError`.
- **التحقق من الطلب:** `buildAIRequest` يرفض الطلبات الناقصة (صورة/برومبت/أبعاد generateImage).
- **التاريخ:** إدراج نتائج AI عبر `engine.addLayer` → يُدفع في سجل التراجع القياسي (غير قابل للتدمير وقابل للتراجع).
- **معالجة الصور القياسية المحلية:** `ImageAnalysisService.analyzeLocal` — إحصاءات بكسل حتمية حقيقية بوسم `origin:"local"`
  (ليست مخرجاً لنموذج AI).

---

## 9) جدول قدرات AI (حسب المعيار الحقيقي — لا تزييف)

أسطورة الحالة: **IMPLEMENTED** = المسار مكتمل (Service + Provider + تنفيذ حقيقي + نتيجة + معالجة أخطاء + التاريخ/التطبيق).
**PARTIAL** = جزئي. **UNAVAILABLE** = غير متاح حالياً دون تزييف. **MISSING** = غير موجود. **BROKEN** = معطّل.

| العملية | الحالة | الملاحظة |
|---|---|---|
| analyzeImage (إحصاء محلي) | **IMPLEMENTED** | إحصاء بكسل حتمي فعلي، origin: local |
| describe / detectObjects / detectFaces / detectText / classifyScene / analyzeComposition | **IMPLEMENTED** (بعيد) | عبر نقطة الإرساء المكوّنة فقط (بروتوكول VS AI) |
| selectSubject / selectObject / segment / removeBackground / removeObject / inpaint / generativeFill / generativeExpand / objectReplace | **IMPLEMENTED** (بعيد) | اختياري: إعادة تطبيق التحديد على محرك التحديد |
| generateImage / generateVariation | **IMPLEMENTED** (بعيد) | يتطلب أبعاداً وبرومبتاً |
| upscale / denoise / sharpen / colorize / restore / relight / enhance | **IMPLEMENTED** (بعيد) | معلمات افتراضية من `paramsForOp` |
| textAssist | **IMPLEMENTED** (بعيد) | أوضاع إعادة الصياغة/الترجمة... |
| segmentProduct / analyzeBackground / detectSurface / estimateLighting / matchLighting / generateShadow / matchPerspective | **IMPLEMENTED** (بعيد) | مخرجات structured تحلل المنتج |
| **جميع العمليات دون نقطة إرساء مكوّنة** | **UNAVAILABLE** (بصدق) | `not-configured` — لا يُزيَّف أي خرج |
| العمليات غير المدعومة من المزوّد المختار | **UNAVAILABLE** (بصدق) | `capability-unavailable` |
| Local AI (استنتاج على الجهاز) | **UNAVAILABLE** (بصدق) | لا يوجد محرك استنتاج مدمج بعد (انظر 11) |

**ملاحظة صريحة:** المزوّد البعيد يعلن دعم العمليات البرمجية الـ34 بروتوكولياً؛ الحقيقة النهائية لدعم كل عملية يقررها
الخادم عبر كل استجابة (ok/error). المزوّد **لا يزوّر** النتائج أبداً — أي فشل يظهر كخطأ `AIError`.

---

## 10) قرار "طبقة AI"

**القرار:** **الاحتفاظ بالتصميم الحالي — لا حاجة لنوع طبقة جديد (`type:"ai"`).**

**السبب:**
- نتائج AI تُدرج كطبقة صورة كاملة القابلية للتحرير تحمل بيانات وصفية `AILayerMetadata` (العملية، المزوّد، النموذج،
  البرومبت، المصدر، التحديد، المعاملات، الأصل `origin`)، انظر `layerFactory.ts:57-84`.
- البيانات الوصفية تُسلسَل إلى صيغة `.vstudio` (`projectFormat.ts:204,213`) وتُعقّم عند التحميل — أي عملية AI مجهولة
  تُسقط ولا تُتلف المستند (اختبار `aiLayer.test.ts` يثبت ذلك لمعالجة ملف مُزوَّر `"operation":"takeover"`).
- السلوك **غير تدميري وقابل للتراجع**: الإدراج عبر `engine.addLayer` يدفع سجل تراجع/إعادة (اختبار تكامل جديد).
- إضافة نوع طبقة جديد تتطلب تعديل اتحاد الأنواع + الرندر + التسلسل + نصوص الترحيل — مخاطرة أعلى مقابل لا قيمة
  وظيفية (الميتاداتا تغطي بالفعل كل ما يُعرض للمستخدم: الوسم، المصدر، المعاملات).

---

## 11) تقرير AI المحلي (الصدق)

- `LocalAIProvider` (الذي يمثّل الاستنتاج على الجهاز) يعلن `capabilities=[]` و`isAvailable()=false` **دائماً**، مع رسالة
  سبب صريحة: "No local inference engine is integrated yet (AI provider unavailable)". (اختبار جديد يثبت ذلك.)
- لوحده، لا يُزعم أبداً في الواجهة "Local AI Ready"؛ الزر المتاح "AI Analysis (local)" يقوم بإحصاء بكسل حتمي حقيقي
  بوسم origin:"local".
- القاعدة: **لا يُنشأ Local AI "جاهز" إلا بوجود محرك استنتاج فعلي + نموذج فعلي.**

---

## 12) تقرير الأمان

- **مفتاح الـ API للبريد ثنائي:** متغير ذاكرة وحيد (`AIService.ts:57 area`) يُضبط عبر `setRemoteApiKey`؛ **لا يُكتب إلى
  localStorage أبداً** (اختبار جديد يفحص محتوى المحل المخزّن وغياب الحقل). يُرسل فقط كترويسة `Authorization: Bearer`.
- **مفتاح التخزين المؤقت:** يتضمن العملية|المزوّد|النموذج|هاش المصدر|هاش التحديد|المعلمات، **دون أي سر**؛ و`AICache`
  طبقية الذاكرة فقط (لا يكُتب للـ localStorage).
- **التعقيم:** ميتاداتا AI تُعقّم عند التحميل؛ الأخطاء تُعرض عبر `AIError.userSafe` دون تسريب تفاصيل داخلية.
- **بوابة المزوّد التجريبي:** Mock غير مسجّل خارج DEV وغير متاح إلا مع `mockEnabled`، وكل نتائجه بوسم `origin:"mock"`
  ودلالات "[MOCK]" — لا يمكن الخلط بينها وبين AI حقيقي (اختبارات جديدة).

---

## 13) جدول الاختبارات (المضافة في هذا النطاق)

| الملف | الاختبارات | ما يغطيه |
|---|---|---|
| `src/editor/tests/beforeAfter.test.ts` | +6 (المجموع 52) | مصفوفة استقرار Before: الضبطيات/التعريض/مورفولوجيا/أقنعة/تبديل المقارنة + تساوي الافتتاح |
| `src/editor/tests/exposure.test.ts` | +8 (المجموع 15) | مسار التشغيل ↔ العامل، الكومبوستر ±1، ثبات المظلم، هوية 0، الحالات القصوى ±100/±200 مع ثبات alpha |
| `src/ai/tests/aiLayer.test.ts` | +2 (المجموع 5) | الإدراج غير التدميري + undo/redo + رفض النتائج بلا صورة |
| `src/ai/tests/honestySecurity.test.ts` | +6 (ملف جديد) | بوابة Mock (DEV+متاح)، صدق Local، وسم origin:"mock"، عدم تثبيت المفتاح، خلو مفتاح التخزين من السر، استقرار المفتاح |

**المجموع النهائي:** 30 ملفاً / 272 اختباراً — **PASS** (من 29/250 في الأساس).

---

## 14) التحقق من النوع والبناء

- `npm run typecheck` → **PASS** (`tsc --noEmit`)
- `npm run test` → **PASS** (30/272)
- `npm run build` → **PASS**:
  - `index` — 474.49 kB (gzip 129.37) — مكافئ للخط الأساسي
  - `processWorker` — 27.55 kB
  - `vendor-react` — 143.56 kB ، `three3d` — 731.67 kB

---

## 15) قائمة الملفات المعدّلة

لم تُعدَّل أي شيفرة إنتاجية (لا حاجة لها). التغييرات كلها في الاختبارات فقط:
1. `src/editor/tests/beforeAfter.test.ts` — أُضيفت مصفوفة الاستقرار وأُصلح اختبار تساوي الافتتاح.
2. `src/editor/tests/exposure.test.ts` — أعيدت كتابته ببنية تعادل موديلات الكانفس `FakeDOMMatrix` وتغطية موحدة للتعريض.
3. `src/ai/tests/aiLayer.test.ts` — أُضيف دسمو "AI result insertion is non-destructive, undoable".
4. `src/ai/tests/honestySecurity.test.ts` — ملف اختبار جديد.

---

## 16) الحدود والملاحظات المتبقية (بصدق)

- تنفيذ العمليات البعيدة يعتمد على نقطة إرساء التكوين من المستخدم؛ دونها تبقى العمليات بإخفاق صريح غير زائف.
- لوحة AI تستخدم معاملات افتراضية ثابتة لكل عملية (لا توجد محررات معاملات مفصّلة لكل عملية).
- `Before` يُعيد الالتقاط عند الاستيراد فقط (لكل مستند مرة واحدة عند الفتح) — هذا سلوك مقصود وموثّق.
- مقدّمة تحرير النصوص/التحويلات البعيدة حساسة لأحجام الردود (لا يوجد حد أقصى لحجم الصورة المرتجعة من الخادم — ملاحظة
  مستقبلية).
- طبقات نتائج AI طبقات بكسل (raster) ولا يوجد اتصال مباشر بـ 3D/Scene من نتيجة AI.

---

## 17) التحقق الوظيفي من الواجهة (وقت التشغيل)

Runtime UI Verification: NOT AVAILABLE
(لا تتوفر أدوات أتمتة متصفح في بيئة التنفيذ الحالية؛ يستند التحقق الوظيفي إلى الاختبارات الآلية المذكورة أعلاه ومراجعة
الكود، وليس إلى تشغيل المتصفح.)

---

## 18) الخلاصة النهائية

أُنجز النطاق المغلق بالكامل: تدقيق ما قبل/بعد ووثقه اختبارات استقرار عبر الميزات؛ التعريض **متّحد رياضياً في كل المسارات
على 2^EV** دون أي تغيير سلوكي وأُقفل باختبارات التطابق والحدود؛ AI موثّق الصدق (لا تزييف، مفتاح السره ميموري،
المزوّد التجريبي محصور بالتطوير، المحلي صريح بأنه غير متاح)؛ طبقة AI اعتبارية عبر ميتاداتا كاملة غير تدميرية
وقابلة للتراجع، ووثقت 34 قدرة AI بجداول صادقة. typecheck + اختبارات (30/272) + بناء الإنتاج كلها خضراء، مع جملة التحقق
الوظيفي وقت التشغيل "NOT AVAILABLE" كما هو مطلوب.

---

**السطر الختامي المطلوب:**
Done. Repaired (6) BEFORE/AFTER, (7) FILTERS + ADJUSTMENTS + EXPOSURE TESTED+VERIFIED, (9) AI (10) AI LAYER TESTED, (11) LOCAL AI HONEST - typecheck PASS, TEST 30/272 PASS, BUILD PASS