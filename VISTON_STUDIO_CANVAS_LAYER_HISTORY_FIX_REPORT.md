# تقرير إصلاح الخلفية الشطرنجية ونمل الساحة وتاريخ بيانات الطبقات (Undo/Redo)

**الملف:** VISTON_STUDIO_CANVAS_LAYER_HISTORY_FIX_REPORT.md
**التاريخ:** 2026-09-21
**المشروع:** D:\VISION STUDIO
**النطاق:** إصلاحات مُغلقة في 3 عناصر فقط: (1) Checkerboard، (2) Marching Ants، (3) Layer Metadata History + Undo/Redo، مع اختبارات، وتحقق آلي، وتقرير، ثم توقف كامل.

---

## 1) ملخص المهمة

- إصلاح خلفية الشطرنج الشفافية (Checkerboard) لتكون ثابتة الحجم في شاشة العرض (Display-only، لا تلمس البكسلات، لا تثبيت في التصدير/الحفظ/التاريخ).
- إصلاح حلقة النمل المتحركة (Marching Ants) لمؤشر التحديد: دورة حياة واحدة لا تسرب، إيقاف ذاتي عند زوال التحديد، إعادة تشغيل نظيفة، إلغاء عند تدمير المحرر.
- ضمان تراجع/إعادة (Undo/Redo) كامل لبيانات الطبقة الخمس: الشفافية، وضع المزج، الرؤية، القفل، والتسمية — دون أي إعادة صورة كاملة أو نسخ Bitmap.
- إضافة اختبارات آلية للأجزاء الثلاثة، وتحقق نهائي من typecheck + tests + build، وتقرير نهائي عربي، ثم التوقف.

## 2) الحالة قبل الإصلاح (Before-State)

- قبل هذه المهمة كانت عناصر نطاقها قد أُصلحت جزئياً في مهمة سابقة (التقرير السابق: `VISTON_STUDIO_REPAIR_AND_VERIFICATION_REPORT.md`). الحالة المسجلة قبل أي تعديل في هذه المهمة (BASELINE):
  - `npm run typecheck` → **PASS**
  - `npm run test` → **27 ملف اختبار / 233 اختباراً — PASS** (المدة 4.12s)
  - `npm run build` → **PASS** (chunk index بحجم 474.03 kB، بلا تحذيرات)
- نواقص مكتشفة خلال مراجعة النطاق مقابل متطلبات المهمة:
  - لا اختبارات لآلية الشطرنج الخالصة (المنطق كان داخل `createCanvasEngine` مغلقاً وغير قابل لاختبار الوحدة).
  - لا اختبارات لدورة حياة حلقة النمل.
  - اختبارات Layer History لا تغطي: التسلسل الكامل للخصائص الخمس، التاريخ المختلط مع تحرير صورة، التشعب (Branching)، والجولة الكاملة Save→Load.

## 3) مشكلة Checkerboard وقرار التصميم

**المشكلة الأصلية (مهمة سابقة):** الحجم كان ينكمش/ينمو مع التكبير (zoom) لأن مقاس البلاطة كان يُشتق من `dpr` (النسبة 1/zoom)، فتظهر شبكة صغيرة جداً عند التكبير العالي وحجمٌ مختلف عند كل مستوى تكبير — سلوك غير معياري.

**قرار التصميم المعتمد (وهو نفس السلوك الذي يُرسَم فعلياً):**
- البلاطات مثبتة في فضاء الشاشة (Screen-space) وليس فضاء المستند: مقاس `CHECKER_SIZE` ثابت = 10px CSS بصرف النظر عن التكبير (25% .. 400%) أو حجم منطقة العرض.
- الطور (Parity) يُحسب من فهارس الخلايا المطلقة `(r + c) % 2` مع تثبيت البداية على شبكة مضاعفات `2*size`، فيفتسب النمط سلساً أثناء التمرير (Pan).
- الرسم Display-only: داخل `render()` فقط بعد تعبئة خلفية المستند، مع قص (Clip) لمساحة المستند، ثم إعادة رسم الكومبوزيت المُخزّن فوقه. لا يؤثر على البكسلات أو التصدير أو الحفظ أو التاريخ.

## 4) إصلاح Checkerboard

- استُخرج المنطق الحسابي إلى دالة خالصة قابلة للاختبار:
  - الملف الجديد: `src/editor/canvas/checker.ts` — يصدّر `checkerTiles(x, y, w, h, size)` ويعيد قائمة `CheckerTile` بنفس خوارزمية الرسم الحالية (لا رسم جديد، لا تغيير سلوكي).
- `drawChecker` في `src/editor/canvas/canvasEngine.ts` يُرسم الآن مباشرة من مخرجات `checkerTiles` (نفس منطق `CHECKER_SIZE`، `CHECKER_A`/`CHECKER_B`، ونفس `+0.5` لتغطية الفجوات).
- هذا لا يُنشئ تطبيقاً مكرراً: `canvasEngine` يحوّل فقط من الحلقتين `for` إلى تمرير واحد على المخرجات الخالصة.
- لا مواعيد عرض إضافية، لا مستندات جديدة، لا تغيير في ملفات الحفظ.

## 5) مشكلة Marching Ants ودورة الحياة

**المتطلبات:** أن تستمر الحركة أثناء سكون المستخدم، وأن تتوقف عند: مسح التحديد، إخفاء التحديد، إغلاق/تغيير المستند، تفكيك المحرر (Unmount)، دون حدوث حلقات `requestAnimationFrame` مكررة أو تسريب.

**الحالة:** كانت الحلقة موجودة (متغير `antsLoopRAF` + دالة `startAntsLoop`) لكن منطقها غير قابل لاختبار الوحدة (مرتبط مباشرة بـ `requestAnimationFrame` داخل الإغلاق)، ولا يوجد إثبات آلي لعدم التكرار/التسريب.

## 6) إصلاح Marching Ants

- وحدة جديدة: `src/editor/canvas/antsTicker.ts` — توفّر `createAntsTicker(opts)` مع حقن `requestFrame/cancelFrame` (قابل للاختبار في node) بمُجمّع منفذ واحد:
  - `start()` يطلب إطاراً واحداً؛ واستدعاؤه المتكرر أثناء التشغيل no-op ⇒ استحالة حلقات مكررة.
  - بعد كل إطار يُعاد الفحص `shouldRun()`: عند زوال التحديد/إخفائه/غياب المستند يتوقف الطائر ذاتياً بلا إطار إضافي.
  - `stop()` يلغي أي إطار معلّق فوراً (تفكيك المحرر / destroy).
  - قابل لإعادة التشغيل دائماً بعد التوقف الذاتي أو `stop()`.
- `canvasEngine` يستخدم الطائر:
  - `shouldRun: () => getDoc() !== null && selectionEngine.hasSelection && !selectionEngine.isHidden()`
  - `onFrame` يعيّن `needsRender = true` ويستدعي `scheduleRender()` فقط (لا `compositeDocument` إطلاقاً أثناء الحركة — يُعاد رسم التراكبات فوق الكومبوزيت المُخزّن).
  - `render()` يستدعي `startAntsLoop()` (المغلّف الرفيع) عند وجود تحديد مرئي؛ `destroy()` يستدعي `antsTicker.stop()` بدل المتغير القديم.
- تم حذف متغير `antsLoopRAF` القديم نهائياً (لا بقايا مراجع).

## 7) Layer Metadata History — الحالة قبل الإصلاح

- عُثر على نظام `MetaSession` قائم في `src/editor/core/engine.ts`: يجمّع تعديلات بيانات الطبقة المتصلة (انزلاق الانشفافية/التحويل) في إدخال بتاريخ واحد، مع تنفيذ فوري (Discrete) للرؤية/القفل/المزج/التسمية.
- طبقة التاريخ `HistoryManager` (في `src/editor/core/history.ts`) تدعم التراجع/الإعادة/القفز مع قصّ ذيل إعادة عند إدخال جديد (Branching) ومخطط حماية أخفاء (Optional).
- لم تتوفر اختبارات تغطي السلوك الكامل (التسلسل، المختلط، التشعب، الجولة). كما أن ادعاءاتها «إدخال لكل عملية» تحتاج إلى فاصلة صريحة بين انزلاق الانشفافية والعملية المنفصلة التالية على نفس الطبقة.

## 8) إصلاح opacity (الشفافية)

- `engine.updateLayerMeta(id, { opacity })` يخلق جلسة `MetaSession` بـ before كامل (بدون نسخ Bitmap) واسم إدخال «Adjust Opacity»، ثم `flushMetaEdit()` (فوري أو بعد 250ms) تنشئ إدخال تراجع واحد لكل سحب منزلق، وإلغاؤه يُعيد الشفافية القديمة فقط.
- اختبارات جديدة:
  - اندماج الانزلاق المتعدد `0.9→0.7→0.5` في إدخال واحد ثم undo→1 ثم redo→0.5 (قائم).
  - `updateLayerMeta` ثم `flushMetaEdit` ثم undo/redo بترتيب معكوس/مباشر ضمن السلسلة الكاملة.

## 9) إصلاح blend mode (وضع المزج)

- التعديل فوري (discrete) واسم إدخاله «Layer Blend Mode»؛ undo يعيد «normal» وredo يعيد الوضع الجديد.
- اختبارات: تسلسل الخصائص الكامل، والتاريخ المختلط، وحالة `multiply`.

## 10) إصلاح visibility (الرؤية)

- التعديل فوري واسم إدخاله «Toggle Visibility»؛ undo يعيد `visible=true`.
- اختبارات: تسلسل كامل + مختلط + تشعب.

## 11) إصلاح lock (القفل)

- التعديل فوري واسم إدخاله «Toggle Lock»؛ undo يعيد `locked=false`.
- اختبارات: تسلسل كامل + مختلط + تشعب (وإثبات أن undo/redo لا يغيِّر مرجع canvas النقطي).

## 12) إصلاح rename (التسمية)

- `renameLayer(id, name)` يمر عبر `makeLayerCommand` لإدخال «Rename Layer» (و `updateLayerMeta(id,{name})` بنفس المسار)، مع حماية القيم المكررة (`if (l.name === name) return`).
- اختبارات: تسلسل كامل + مختلط (اسم يعود إلى الاسم الأصلي بعد undo).

## 13) Undo/Redo عبر MetaSession

- `pushHistory` (engine.ts) يفرّغ أي جلسة معلّقة قبل أي إدخال جديد ⇒ ترتيب صارم بين إدخالات الصورة وإدخالات البيانات.
- `undo()/redo()` يستدعيان `flushMetaEdit()` أولاً ثم `history.undo()/redo()` ثم `requestRender()` و`syncHistoryView()`.
- اختبار «التاريخ المختلط» يثبت أن تحرير صورة (Brush Stroke) يبقى مرتّباً مع 5 إدخالات بيانات ويدخل/يخرج من التراجع بلا أخطاء، ويُعاد تطبيقه بالكامل عبر redo.

## 14) اندماج التغيير المتصل (Coalescing) عند الانزلاق

- الانزلاق المتصل (opacity / transform) يُدمج في إدخال واحد بشرط نفس الطبقة ونفس «مفتاح» الجلسة؛ العمليات المنفصلة (blend/visible/lock/name) تُزال فورياً.
- عند الحاجة لفصل صريح بين جرعة انزلاق متصلة وعملية منفصلة تالية على نفس الطبقة (مثل تدفق واجهة المستخدم الطبيعية)، توجد واجهة عامة `flushMetaEdit()`، وقد استُخدمت في الاختبارات كنقطة فاصلة طبيعية (نهاية السحب).
- الاختبارات تغطي الاندماج (0.9→0.7→0.5 = إدخال واحد) وعدم الاندماج عند تغيّر الطبقة والعملية.

## 15) ID-based Layer Undo/Redo (أمان معرف الطبقة)

- `makeLayerCommand` تبحث عن الطبقة بالمعرف (`Map` مبني على `l.id`) وتضبط قيم `before/after` للمعرف نفسه؛ لا «تنفيذ» عام على المستند كله.
- معالجة `imageId` (تبديل مستويات البكسلات) تُنفَّذ فقط عندما يتغيّر `imageId` فعلاً بين before/after (عمليات transform/crop مثل تدوير/تغيير الأبعاد). تعديلات البيانات (opacity/blend/visible/lock/rename) لا تُغيّر imageId ⇒ لا تُلمس البكسلات إطلاقاً عند undo/redo.
- اختبار «لا يلمس البكسلات» يثبت أن مرجع `pixelStore.get(imageId)` يبقى كما هو بعد undo ثم redo.

## 16) الاختبارات المضافة

| الملف | الميزة |
|---|---|
| `src/editor/tests/checker.test.ts` (5 اختبارات) | ثبات مقاس البلاطة مع أي حجم منطقة (متصل بالتكبير)، تناوب صارم أفقياً/رأسياً، تغطية كاملة بلا فجوات، ثبات الطور عند التمرير (Pan)، الحالات الهامشية (أبعاد/أحجام صفرية أو سالبة أو NaN). |
| `src/editor/tests/antsTicker.test.ts` (7 اختبارات) | حلقة واحدة فقط رغم تكرار `start()`، لا بدء عندما `shouldRun=false`، نبض إطار واحد ثم إعادة تزويد بالضبط، إيقاف ذاتي عند زوال التحديد، `stop()` يلغي الإطار المعلق، إعادة تشغيل بعد التوقف، تفكيك بلا إطارات لاحقة. |
| `src/editor/tests/layerMetaHistory.test.ts` (+5 اختبارات) | التسلسل الكامل لخصائص الطبقة الخمس (undo معكوس / redo مباشر)، لا لمس للبكسلات في إدخالات البيانات، التاريخ المختلط مع Brush Stroke + selection، التشعب (undo ثم إدخال جديد يقصّ ذيل redo)، جولة Save→Load تحافظ على opacity 0.5 + multiply + invisible + locked + name «Main». |

تم الاحتفاظ بجميع الاختبارات السابقة كما هي (لم يُحذف أي اختبار/ميزة).

## 17) التحقق الآلي (الأرقام)

- **BASELINE (قبل التعديلات):** typecheck PASS · 27 ملف / 233 اختبار PASS · build PASS (index 474.03 kB).
- **بعد الإصلاحات:**
  - `npm run typecheck` → **PASS** (لا أخطاء، بدون `any`/`@ts-ignore`).
  - `npm run test` → **29 ملف / 250 اختباراً — PASS** (المدة 3.56s).
  - `npm run build` → **PASS** (148 وحدة؛ index 474.49 kB — نعم `tsc --noEmit && vite build`).

## 18) التحقق عبر الواجهة (Runtime UI)

- **غير متاح (NOT AVAILABLE).** لا متصفح/أتمتة واجهة متاحة في بيئة العمل الحالية. التحقق من السلوك البصري (نمط الشطرنج تحت التكبير والتمرير، حركة النمل)، ومن عدم وجود حلقات مكررة في متصفح حقيقي، يبقى مطلوباً كخطوة تحقق يدوية لاحقة. لا يُدّعى نجاح بصري غير مثبت آلياً.

## 19) الملفات المعدّلة/المضافة/المحذوفة

**مُعدَّل:**
- `src/editor/canvas/canvasEngine.ts` — `drawChecker` يستخدم `checkerTiles`؛ حلقة النمل عبر `antsTicker` (حذف `antsLoopRAF` القديم، `destroy()` ← `antsTicker.stop()`).
- `src/editor/tests/layerMetaHistory.test.ts` — +5 اختبارات (تسلسل كامل، لا لمس بكسلات، مختلط، تشعب، Save/Load) وامتداد mock الـ canvas (`beginPath/rect/clip`) لممارسة مسار الفرشاة الحقيقي.

**مُضاف:**
- `src/editor/canvas/checker.ts`.
- `src/editor/canvas/antsTicker.ts`.
- `src/editor/tests/checker.test.ts`.
- `src/editor/tests/antsTicker.test.ts`.

**محذوف في هذه المهمة:** لا شيء.

**خارج نطاق هذه المهمة (لم يُمسّ):** إعداد `vite.config.ts` (manualChunks من مهمة سابقة — هذه المهمة لم تُنفِّذ أي Code Splitting)، وملفات التقارير السابقة، وملفات الاختبارات الأخرى، وpackage.json/tsconfig/package-lock.

## 20) المشاكل المتبقية والنتيجة النهائية

- **لا مشاكل صحيحة معروفة متبقية** ضمن النطاق الثلاثي بعد الإصلاحات. كل متطلبات المهمة الثلاثة مغطاة باختبارات آلية ناجحة.
- المتبقي الوحيد: التحقق البصري اليدوي في المتصفح (القسم 18) — غير متاح في هذه البيئة.
- مذكرتان:
  - تتطلب سيناريوهات «مسح/إضافة تحديد» و«تغيير تكبير أو مستند» من النمل إعادة التشغيل عبر `start()` الذي يستدعيه `render()` عند ظهور تحديد مرئي — مغطى بالتصميم والاختبارات.
  - قواعد النطاق المطلوبة (لا تطبيقات مكررة، لا حذف اختبارات/ميزات، لا `as any` لإخفاء الأخطاء، لا تغيير إصدارات الحزم/التبعيات) محترمة بالكامل.

---
**الملخص النهائي المحقق آلياً:** Checkerboard FIXED · Marching Ants FIXED · Layer Metadata History (opacity/blend/visible/lock/rename) FIXED · Undo/Redo PASS · Mixed History PASS · Save/Load PASS · Typecheck PASS · Tests 250/250 (29 ملفاً) · Build PASS · Runtime UI Verification NOT AVAILABLE.